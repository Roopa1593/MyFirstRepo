#include<stdio.h>
#include<conio.h>
void main()
{
	int num,res;
		clrscr();
			scanf("Enter the Number %d",&num);
				res=fact(n);
					printf("Factorial : %d",res);
}						getch();
fact(n)
{
	if num = 0 ||1 
			return 1;
				else 
					
							return fact(n-1);
}
